<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name="com.habitao.app.HabitaoApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Habitao">

        <activity
            android:name="com.habitao.app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Habitao.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.habitao.system.notifications.HabitReminderReceiver"
            android:exported="false" />

        <receiver
            android:name="com.habitao.system.notifications.HabitCompletionReceiver"
            android:exported="false" />

        <receiver
            android:name="com.habitao.system.notifications.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.habitao.system.notifications.TaskReminderReceiver"
            android:exported="false" />

        <receiver
            android:name="com.habitao.system.notifications.TaskCompletionReceiver"
            android:exported="false" />

        <service
            android:name="com.habitao.feature.pomodoro.service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Pomodoro Timer" />
        </service>

    </application>

</manifest>
