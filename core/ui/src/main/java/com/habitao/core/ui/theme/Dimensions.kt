package com.habitao.core.ui.theme

import androidx.compose.ui.unit.dp

// Reusable spacing and dimension constants for consistent layout

object Dimensions {
    // Screen-level padding
    val screenPaddingHorizontal = 16.dp
    val screenPaddingVertical = 16.dp

    // Card dimensions
    val cardPadding = 16.dp
    val cardSpacing = 12.dp
    val cardElevation = 2.dp
    val cardElevationCompleted = 0.dp

    // Section spacing (between form sections, header and list, etc.)
    val sectionSpacing = 24.dp

    // Element spacing within components
    val elementSpacing = 8.dp
    val elementSpacingSmall = 4.dp
    val elementSpacingLarge = 12.dp

    // Icon sizes
    val iconSizeSmall = 18.dp
    val iconSizeMedium = 20.dp
    val iconSizeLarge = 24.dp

    // Action button sizes
    val actionButtonPrimary = 40.dp
    val actionButtonSecondary = 34.dp

    // Progress bar
    val progressBarHeight = 6.dp
    val progressBarHeightLarge = 8.dp

    // Bottom spacer (for FAB clearance)
    val fabClearance = 88.dp

    // Task list
    val subtaskIndentPerLevel = 32.dp
    val taskRowVerticalPadding = 2.dp

    // About screen
    val logoSize = 120.dp
}
